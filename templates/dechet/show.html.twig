{% extends 'base.html.twig' %}

{% block title %}Détails du Déchet{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Détails du Déchet</h2>
        <div>
            <a href="{{ path('app_dechet_index') }}" class="btn btn-secondary me-2">
                <i class="bi bi-arrow-left me-2"></i> Retour à la liste
            </a>
            <a href="{{ path('app_dechet_edit', {'id': dechet.id}) }}" class="btn btn-warning me-2">
                <i class="bi bi-pencil me-2"></i> Modifier
            </a>
            <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
                <i class="bi bi-trash me-2"></i> Supprimer
            </button>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Type</label>
                        <p>{{ dechet.type }}</p>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Quantité</label>
                        <p>{{ dechet.quantite }} kg</p>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="mb-3">
                        <label class="form-label fw-bold">Zone</label>
                        <p>{{ dechet.zone.nom }}</p>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold">Description</label>
                        <p>{{ dechet.description }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal de suppression -->
    <div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Supprimer le déchet</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    Êtes-vous sûr de vouloir supprimer ce déchet ? Cette action est irréversible.
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Annuler</button>
                    {{ form_start(delete_form) }}
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash me-2"></i> Supprimer
                    </button>
                    {{ form_end(delete_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
